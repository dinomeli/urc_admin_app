import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF00ADB5); // آبی اصلی
  static const background = Color(0xFFF4F6F9);
  static const sidebarBackground = LinearGradient(
    begin: Alignment.topCenter,
    end: Alignment.bottomCenter,
    colors: [Color(0xFF30475E), Color(0xFF222831)],
  );
  static const textLight = Color(0xFFCFD3D8);
  static const white = Colors.white;
}